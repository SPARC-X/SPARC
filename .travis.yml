sudo: true
language: Generic
os:
  - linux
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - build-essential
    - gfortran
    - gcc
    - mpich
    - liblapack-dev
    - liblapacke-dev
    - libmpich-dev
    - libopenblas-dev
    - mpich
install:
  - cd src/; make clean; make #2> makeLog
jobs:
  include:
    -stage:tests
    script: travis_wait 60 make test1 # already in src/ during install 
    script: travis_wait 60 make test2 # already in src/ during install 
